export const template = `
        <div class="popup">
        
          <input name="{{name}}" class="form-input" type="{{type}}" 
          value="{{value}}" placeholder="{{placeholder}}">
          
          <span class="popuptext"> </span>
        </div>
`
